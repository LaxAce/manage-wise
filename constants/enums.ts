export enum EContextMenuPosition {
    LEFT = "left",
    RIGHT = "right",
    CENTER = "center",
}

export enum EDropdownPosition {
    TOP = "top",
    BOTTOM = "bottom",
}
export enum EnvironmentStageEnum {
    PROD = "production",
    DEV = "development",
};

export enum ResponseStatusEnum {
    ERROR = "error",
    SUCCESS = "success",
}

export enum CookiesEnum {
    AUTH_TOKEN = "authToken", 
}

export enum UserTypeEnum {
  ADMIN = "ADMIN",
  USER = "USER",
}
